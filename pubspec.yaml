name: moashir
description: MO'ASHIR - Your health, simplified.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0+2
  camera_platform_interface: ^2.13.1
  camera_web: ^0.3.5+4
  cupertino_icons: ^1.0.6
  google_fonts: ^6.2.1
  google_mlkit_pose_detection: ^0.15.0
  permission_handler: ^11.3.1
  web: ^1.1.1
  http: ^1.6.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
